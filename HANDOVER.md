# HANDOVER.md: the MULTI-OD worktree (branch `gen29-multiod`), CLOSED

> Rewritten 2026-08-04 in the project-wide documentation refactor. **This branch is closed;
> nothing is running and nothing is pending.** The master project state, claims register and
> wording rules live in `../sacred/HANDOVER.md`; read `CLAUDE.md` first for identity, house
> rules and the project map. Citable numbers live only in this worktree's `experiments/`
> ledgers.

## What this branch holds

The closing experiment of the boundary map (three-stream coordination) and its two follow-ups.

| act | verdict | ledger |
|---|---|---|
| gen29 | The ORACLE half banks the project's only gap that survives a complete hostile baseline family (median 31% below the oracle-fitted m-pairing cap over 55 screened cells; prevalence figure `assets/gen29_prevalence.png`). The TRAINED half FAILED both tiers 0/3 seeds, with the blinded control EQUAL to the sighted policy, so the coordination channel carried nothing | `experiments/gen29_multiod.md` |
| gen36 | The pre-committed rescue pair. Distillation with EXACT coordinated labels fails both tiers 0/3 (CE plateaus far above the label-entropy floor), separating the wall as CONDITIONING CAPACITY, not training dynamics; the dense-credit re-aim was correctly not launched per the pre-registered gate | `experiments/gen36_multiod_rescue.md` |
| gen37 | LLM route curation REJECTED at the ceiling (the LLM shortlists route-triples worse than random at every prune size, oracle-exact); the trained half aborted on Kilian's instruction, restriction-unlocks-SACRED logged as future work only | `experiments/gen37_reasoning_curation.md` |

## The thesis sentence this branch earns

Below a measurable boundary, contested routing needs no learning; in the one register where no
hand-built rule, however oracle-assisted, can express the optimal defence, model-free
adversarial self-play at thesis scale also fails to capture it, and the mechanism is
conditioning capacity, measured by a distillation control with exact labels and a blinded
causal control. The boundary map closes with a measured edge.

## Housekeeping notes

- The build brief `GEN29_MULTIOD_HANDOFF.md` and this tree's stale copies of the roads-era
  documentation are in `docs/archive/` (see its `INDEX.md`).
- The chronicle lives on the roads worktree (`../sacred/SACRED_PROGRESS.md`, entries 27, 32,
  33 cover this branch); this tree's `SACRED_PROGRESS.md` is a pointer stub.
- The `b2_llm_benchmark`, `gen19`, `gen27` and `regime_decision_table` ledger copies here are
  DIVERGED from the roads copies, which carry the corrected-yardstick appendices and are
  authoritative; cite from roads.
- Suite last recorded 173 green. Any new work on this branch would start from the master
  HANDOVER, not from here.
